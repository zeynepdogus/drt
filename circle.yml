machine:
  java:
    version: oraclejdk8
  environment:
    toolset_branch: master
    server_type: circle
    used_modules: check_cache,code_style_check,unit_tests
general:
  artifacts:
    - "build/libs/*.jar”

  
